let price = 25;
//let taxrate = 0.07;

//price = price + 1;
//price = price - 1;
//price = price / 5;
//price = price * 3;
//price = price % 4;

// showMessage(++price);    Increases price by 1 and shows it
// showMessage(price++);    Shows price original value and then increases it by 1

//showMessage((price * taxrate).toFixed(2));    Fixing decimals to .00



showMessage(price);